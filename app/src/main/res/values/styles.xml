<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/blue_bright</item>
    </style>

    <style name="OverlayDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="MyTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToggleSwitchButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="outerRadius">130dp</item>
        <item name="handleDrawable">@drawable/ic_btn_center</item>
        <item name="upper">@drawable/ic_unlock</item>
        <item name="bottom">@drawable/ic_browser</item>
    </style>

    <style name="ToggleSwitchButtonVideo">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="outerRadius">130dp</item>
        <item name="handleDrawable">@drawable/ic_btn_center</item>
        <item name="upper">@drawable/ic_unlock</item>
        <item name="bottom">@drawable/ic_video</item>
    </style>

    <style name="ToggleSwitchButtonCall">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="outerRadius">130dp</item>
        <item name="handleDrawable">@drawable/ic_btn_center</item>
        <item name="upper">@drawable/ic_unlock</item>
        <item name="bottom">@drawable/ic_call</item>
    </style>

    <style name="ToggleSwitchButtonInstall">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="outerRadius">130dp</item>
        <item name="handleDrawable">@drawable/ic_btn_center</item>
        <item name="upper">@drawable/ic_unlock</item>
        <item name="bottom">@drawable/ic_install</item>
    </style>

    <style name="ToggleSwitchButtonDefult">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="outerRadius">130dp</item>
        <item name="handleDrawable">@drawable/ic_btn_center</item>
        <item name="upper">@drawable/ic_unlock</item>
        <item name="bottom">@drawable/ic_browser</item>
    </style>

    <style name="MyTabTheme" parent="MyTabTheme.Base">

    </style>

    <style name="MyTabTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar" tools:ignore="NewApi">false</item>
        <item name="colorPrimary">#E0EDEE</item>
        <item name="colorPrimaryDark">#c1eaed</item>
        <item name="colorAccent">#231F20</item>
    </style>

    <!-- Base application theme. -->
    <style name="MyAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/blue_bright</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="MyRadioButtonIntro" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="x" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
